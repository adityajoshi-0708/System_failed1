# GitHub Token for AI Models
GITHUB_TOKEN=you_api_key

# bear token
Bearer_Token=authenticate_token

# Pinecone Configuration
PINECONE_API_KEY=your_api

# FIXED: Pinecone Index Hosts (matching your dashboard)
CHUNKS_INDEX_HOST=your_api
CACHE_INDEX_HOST=you_api_key


# =============================================================================
# OPTIONAL PERFORMANCE TUNING (Defaults work great!)
# =============================================================================

# Maximum concurrent requests (Railway default: 50)
MAX_CONCURRENT_REQUESTS=50

# Embedding batch size for processing (Railway default: 100)
EMBEDDING_BATCH_SIZE=100

# Use local embeddings for 384-dim compatibility (Railway default: true)
USE_LOCAL_EMBEDDINGS=true

# Text chunk size for processing (Railway default: 800)
CHUNK_SIZE=800

# Number of chunks to retrieve (Railway default: 2)
TOP_K_RETRIEVAL=2

MAX_TOKEN_RESPONSE=80
EMBEDDING_CACHE_SIZE=10000
ANSWER_CACHE_SIZE=5000

# =============================================================================
# RAILWAY DEPLOYMENT NOTES
# =============================================================================
# 1. Create a Railway account at railway.app
# 2. Connect your GitHub repository
# 3. Add these environment variables in Railway dashboard
# 4. Railway will automatically deploy using the Procfile
# 5. Your API will be available at: https://your-app.railway.app